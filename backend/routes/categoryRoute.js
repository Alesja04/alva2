import express from "express";

const categoryroute = express.Router();

// categoryroute.post("/", Login);
// categoryroute.get("/", Login);
// categoryroute.get("/", Login);
// categoryroute.patch("/", Login);
// categoryroute.delete("/", Logout);

export default categoryroute;
